language: python
python:
  - "3.8"
services: docker

before_install:
  - sudo apt-get update
cache: pip

install:
  - pip install -r requirements.txt
  - pip install -e .
script:
  - flake8 src/Index.py

after_success:
  - git config --global user.name "Georgi768"
  - git config --global user.email "goskate004@gmail.com"
  - git add --all git commit -m "Testing"
  - git remote add origin http://${GH_TOKEN}@github.com/Georgi768/TravisValidation.git
  - git push -u origin main